# JS/TS workspace members. The Python app (apps/api) is managed by uv, not pnpm.
packages:
  - apps/web
  - packages/shared

# Build scripts pnpm should not run automatically (require explicit approval).
ignoredBuiltDependencies:
  - sharp
  - unrs-resolver
